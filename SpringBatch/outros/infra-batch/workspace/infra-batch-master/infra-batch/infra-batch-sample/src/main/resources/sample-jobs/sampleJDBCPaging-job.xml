<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:batch="http://www.springframework.org/schema/batch" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.2.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.2.xsd
		http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-4.2.xsd
		http://www.springframework.org/schema/batch http://www.springframework.org/schema/batch/spring-batch-3.0.xsd">

	<!-- Exemplo de Job que faz paginação na leitura dos dados do banco de dados para não sobrecarregar a memória -->

	<beans:import
		resource="classpath:/spring/context/infra/batch/infra-batch-context.xml" />

	<batch:job id="sampleJDBCPagingJob" parent="infraParentJob"
		restartable="false">
		<batch:step id="step1" parent="infraParentStep" next="step2">
			<batch:tasklet ref="sampleTasklet" task-executor="syncTaskExecutor" />
		</batch:step>
		<batch:step id="step2" parent="infraParentChunkedStep">
			<batch:tasklet task-executor="threadPoolTaskExecutor"
				throttle-limit="${infra.tasklet.throttle-limit}">
				<batch:chunk reader="sampleSynchronizedReader"
					processor="sampleProcessor" writer="sampleWriter">
				</batch:chunk>
			</batch:tasklet>
		</batch:step>
	</batch:job>

	<beans:bean name="sampleTasklet"
		class="br.gov.serpro.supde.infra.batch.sample.SampleTasklet" />

	<beans:bean id="sampleSynchronizedReader"
		class="br.gov.serpro.supde.infra.batch.core.concurrent.SynchronizedItemReader"
		scope="step">
		<beans:property name="delegate" ref="sampleJDBCPagingReader" />
	</beans:bean>

	<beans:bean id="sampleJDBCPagingReader"
		class="org.springframework.batch.item.database.JdbcPagingItemReader">
		<beans:property name="dataSource" ref="dataSource" />
		<beans:property name="saveState" value="false" />
		<beans:property name="queryProvider">
			<beans:bean
				class="org.springframework.batch.item.database.support.SqlPagingQueryProviderFactoryBean">
				<beans:property name="dataSource" ref="dataSource" />
				<beans:property name="selectClause"				 			
					value="SELECT id, name, country, birthdate, company, depto, extra" />
				<beans:property name="fromClause" value="FROM experience" />
				<beans:property name="sortKey" value="ID" />
			</beans:bean>
		</beans:property>
		<beans:property name="pageSize" value="5" />
		<beans:property name="rowMapper">
			<beans:bean
				class="br.gov.serpro.supde.infra.batch.sample.domain.ExperienceRowMapper" />
		</beans:property>
	</beans:bean>

	<beans:bean id="sampleProcessor"
		class="br.gov.serpro.supde.infra.batch.sample.SampleProcessor" />

	<beans:bean id="sampleWriter"
		class="br.gov.serpro.supde.infra.batch.sample.SampleWriter" />

</beans:beans>